{
  "name": "nvim-docker (web)",
  "build": {
    "context": "../../devcontainer-conf",
    "dockerfile": "../../devcontainer-conf/Dockerfile",
    "target": "web"
  },
  "remoteUser": "dev",
  "overrideCommand": false,
  "postCreateCommand": "sh -lc \"nvim --headless '+Lazy! sync' +qa || true\"",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  }
}
